= image_tag @event.background.url, class: "bg-img"
#container
	%h1 GreenUP Travel App
	%h2 Calculate your travel footprint to #{@event.name}
	
	= form_for @user, url: event_users_path do |f|
		%ul
			%li
				= f.text_field(:name, autofocus: true, placeholder: 'Name')
			%li
				= f.email_field(:email, placeholder: 'Email')
			#email_input_response

