#container
	%p 
		We recommend that you donate 
		%span $#{@money_amount}
		to compensate for your travels.

	=link_to("Give a tree", new_tree_path(user_id: params[:user_id], event_id: params[:event_id]))
	%br
	=link_to("Make a pledge", new_pledge_path(user_id: params[:user_id], event_id: params[:event_id]))

%style
	body {
	background: url(#{Event.find(params[:event_id]).attachment})
	}