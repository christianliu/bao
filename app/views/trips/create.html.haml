= image_tag @event.background.url, class: "bg-img"
#container
	#money-message
		According to our calculations, you could GreenUp your travels by contributing
		#money-amount= Price.new(@money_amount).to_s
		towards green projects.
  
	=link_to("Contribute towards planting a tree", new_event_tree_path(@event, amount: @money_amount))
	%br
	=link_to("Make your own green pledge", new_event_pledge_path(@event))
